
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stored information about ANFLOCOR employees.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["employee", "manager", "admin"] }
      },
      "required": ["uid", "email"]
    },
    "CustomerMapping": {
      "title": "Customer Mapping",
      "description": "Mapping of customers to SAP production channels.",
      "type": "object",
      "properties": {
        "customerID": { "type": "string" },
        "customer": { "type": "string" },
        "sapcCode": { "type": "string" },
        "sapcDesc": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["customerID", "customer"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "User profile document"
    },
    "/customerMappings/{mappingId}": {
      "schema": { "$ref": "#/entities/CustomerMapping" },
      "description": "Customer mapping records"
    }
  }
}
